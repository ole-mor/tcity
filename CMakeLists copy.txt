cmake_minimum_required(VERSION 3.10)
project(tcity)

set(CMAKE_CXX_STANDARD 11)

# Add source files
set(SOURCES
    src/main.cpp
    src/LoadModel.cpp
    src/RenderGLTF.cpp
)

# Add executable
add_executable(tcity ${SOURCES})

# Include directories
target_include_directories(tcity PUBLIC include)

# Link libraries
target_link_libraries(tcity glad glfw) # Add other libraries as necessary
